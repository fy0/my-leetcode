
这道题经过了一番复杂的折腾。

首先题目给的例子就比较片面，一开始以为字符的形状是“H”的造型，一个隔一个。其中横线是单行的。

三行的如此解掉了，然后四行的测试用例出了问题，于是又将横线加粗成两行。

最终才知道整体是锯齿状的……

========

这里又有一个发现

因为是打散重组字符串s，最后返回
我一开始用
char buf[len];
最后
memcpy(s, buf, len);
return s;

以为这种避免申请内存会快些
结果发现 char *buf = malloc(len+1);
比那种快了好多
我以为栈+memcpy要比堆上的变量要快
但并不是这样

========

后面觉得 main.c 还能改进一下，于是做了 main1a.c 的优化

从 22ms 优化到了 42ms

。。。。
